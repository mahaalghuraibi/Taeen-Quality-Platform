# عين الجودة — متطلبات الوكيل المحلي (Local AI Agent)
# Python 3.10+ مطلوب.
#
# GPU (NVIDIA/CUDA): ثبّت PyTorch المتوافق مع إصدار CUDA لديك من:
#   https://pytorch.org/get-started/locally/
# مثال CUDA 12.1:
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu121
# CPU فقط (للاختبار):
#   pip install torch torchvision
#
# ثم ثبّت بقية المتطلبات: pip install -r requirements.txt

ultralytics>=8.1.0
opencv-python>=4.8.0
PyYAML>=6.0
requests>=2.31.0
numpy>=1.24.0
Pillow>=10.0.0

# torch / torchvision تُثبّت يدوياً حسب CUDA (انظر الأعلى).
# torch>=2.1.0
# torchvision>=0.16.0
