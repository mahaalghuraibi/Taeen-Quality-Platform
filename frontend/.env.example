# Backend API origin (no trailing slash). Required when the SPA is hosted separately from the API.
# Example (Render):
# Required on Render Static Site (Environment → Build):
# VITE_API_BASE_URL=https://taeen-quality-platform.onrender.com
#
# Leave unset for local dev — Vite dev server proxies /api to the backend.
VITE_API_BASE_URL=
